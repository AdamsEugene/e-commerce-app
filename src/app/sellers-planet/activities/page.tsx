import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Activities",
};

export default function Activities() {
  return (
    <div className="home">
      <div className="main">Activities</div>
    </div>
  );
}
