import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Add Products",
};

export default function AddProducts() {
  return (
    <div className="home">
      <div className="main">AddProducts</div>
    </div>
  );
}
